import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Fron-end-test-converter',
  webDir: 'www'
};

export default config;
